import 'package:flutter/cupertino.dart';

class IRadio extends StatelessWidget {
  const IRadio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
